# Frontend — injetado pelo Next.js no bundle (tempo de build)
NEXT_PUBLIC_SENTRY_DSN=

# Rust/Tauri — lido em tempo de compilação via option_env!()
# Deve estar definido no ambiente antes de: cargo build / pnpm run build
SENTRY_DSN=
